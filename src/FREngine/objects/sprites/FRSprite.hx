package frEngine.objects.sprites;

import openfl.display.Sprite;

/**
 * A sprite of the Flash Rhythm Engine.
 * @author Nimblemations
 */

class FRSprite extends Sprite {
	
	/**
	 * Called on frame.
	 * @param elapsed How much time has elapsed.
	 */
	public function update(deltaTime:Float) {
		
	}
	
	/**
	 * This function is called when added to (usually) an Array, whatever added it has to call this function.
	 * (I'll come up with a better solution later)
	 */
	public function add() {
		
	}
}